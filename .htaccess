RewriteEngine On
RewriteBase /AJAX/

# Si es archivo o carpeta existente, no redirigir
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Si no existe, enviar al index.php
RewriteRule ^ index.php [QSA,L]
